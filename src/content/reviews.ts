/**
 * Отзывы родителей.
 *
 * СЕЙЧАС СПИСОК ПУСТ, И БЛОК НА САЙТЕ НЕ ПОКАЗЫВАЕТСЯ. Это решение
 * владельца от 31 августа 2026: выставим, когда накопятся настоящие.
 *
 * Что здесь было и почему убрано. Лежали шесть отзывов, перенесённых со
 * старого сайта, а секция была подписана «Отзывы опубликованы
 * пользователями на Яндекс Картах» и вела ссылкой на карточку школы.
 * Проверка карточки 31 августа 2026 показала: на ней **один** отзыв
 * (Елена П от 19 августа), и ни один из шести с ним не совпадает.
 * То есть сайт называл источник, в котором этих текстов нет, — родитель
 * убеждался в этом одним кликом по нашей же ссылке.
 *
 * Косвенные признаки, что тексты относились к другой школе: «тренеры» во
 * множественном числе, хотя занятия ведёт Ярослав лично; «зимние
 * каникулы» и «лагерь», которых у школы в контенте нет; «старший сын
 * занимался с пяти до шестнадцати лет» при новой школе с набором от 7 лет.
 * Разбор целиком — в docs/yandex-business.md.
 *
 * КАК ВЕРНУТЬ БЛОК, когда отзывы появятся:
 *
 *   1. Добавить объекты в массив ниже — дословно, как опубликовано в
 *      источнике. Не редактировать и не сокращать чужой текст.
 *   2. Вернуть <Reviews limit={4} /> в src/app/page.tsx между
 *      GalleryPreview и PoolPreview.
 *   3. Проверить, что reviewsSource действительно ведёт туда, где эти
 *      отзывы видны. Если источник другой — поменять ссылку, а не подпись.
 *
 * Не добавляйте сюда тексты, которых нет в публичных источниках, и не
 * переносите отзывы другой школы. Выдуманный или чужой отзыв — это не
 * «слабое место сайта», а недостоверная реклама услуги для детей.
 */

import { contacts } from './contacts';

export type Review = {
  id: string;
  text: string;
  author: string;
  /** Контекст отзыва — о чём именно он */
  context: string;
};

export const reviews: Review[] = [];

export const reviewsSource = {
  label: 'Публичные отзывы на Яндекс Картах',
  href: contacts.address.yandexMaps,
};
