# Цели в Яндекс Метрике

Код отправляет события, но **сами цели создаются в интерфейсе Метрики** —
без этого события уходят в пустоту и в отчётах их не будет.

Счётчик **111745879** («школа PRIMESWIM»). Источник имён —
`src/lib/analytics.ts`, тип `AnalyticsGoal`. Если правите имя в коде —
правьте и цель, иначе она замолчит.

## Состояние на 1 сентября 2026

Кабинет приведён в порядок: у каждой цели, которую отправлял код на тот
момент, есть своя цель в Метрике. Использовано 14 из 200.

**Одна цель с тех пор появилась в коде и в кабинете её ещё нет** —
`lead_delivered`, см. раздел ниже. Пока она не заведена, отправка заявки в
отчётах не считается.

| Цель | ID | Идентификатор | Условие |
|---|---|---|---|
| Написать в Telegram (запись) | 604219347 | `click_telegram_booking` | Совпадает |
| Написать в MAX (запись) | 604219735 | `click_max_booking` | Совпадает |
| Открыл Telegram-канал | 604220092 | `click_telegram_channel` | Совпадает |
| Открыл канал в MAX | 604220330 | `click_max_channel` | Совпадает |
| Начал заполнять форму | 604220699 | `start_form` | Совпадает |
| Открыл ВКонтакте | 604498645 | `click_vk` | Совпадает |
| Дошёл до контактов | 604498899 | `view_contacts` | Совпадает |

Условие везде «Совпадает», а не «Содержит». Это важно: при «Содержит»
цель `click_max` ловила бы и `click_max_booking`, и `click_max_channel`,
и разведение записи и канала, ради которого всё делалось, не сработало бы.

Уже были в кабинете и продолжают работать: `cta_booking` (ID 601236151),
`cta_schedule` (601236267), `click_route` (601236370).

Цель 601236267 переименована из «Клик "Посмотреть программу"» в «Клик
"Расписание занятий"»: кнопка на сайте называется так с 31 августа.
Идентификатор `cta_schedule` не тронут, поэтому вся история по цели
сохранилась — менялась только подпись в отчёте.

`click_phone` отдельной ручной цели не имеет и не нуждается: её полностью
закрывает автоцель Яндекса «клик по номеру телефона» (ID 599664690).

**Старые цели не удалены — по ним осталась история.** Данных они больше не
получают: `lead_submitted` (601235944) код не отправляет вовсе,
`open_telegram_draft` (604218647) перестала получать данные 3 сентября 2026,
когда форма научилась отправлять заявку по-настоящему, а `click_telegram`
(601237068) и `click_max` (601237123) заменены парами `_booking` /
`_channel`. Когда история перестанет быть нужна, их можно скрыть.

## Что нужно завести после запуска приёма заявок

**`lead_delivered`** — тип «JavaScript-событие», идентификатор ровно
`lead_delivered`, условие «Совпадает». Название для отчёта: «Заявка
доставлена».

Это первая честная цель «заявка» за всё время существования сайта: событие
отправляется только после ответа функции, подтвердившего, что сообщение ушло
в MAX и у заявки есть номер (см. `lead-delivery.md`). Не по клику и не по
показу экрана «спасибо».

Пока цель не заведена в кабинете, событие уходит в пустоту — код его
отправляет, но в отчётах его не будет.

### Что знать про автоцели

Автоцель «переход в мессенджер» (599545130) считает все мессенджеры одной
кучей и не различает запись и канал — то самое смешение, ради разделения
которого заведены пары `_booking` / `_channel`. Для честной атрибуции
смотреть ручные цели, а не её.

Автоцели «Яндекс Бизнес: заявка» (600097531 и 600097532) приходят из
интеграции с карточкой организации, а не с сайта.

### Проверено на боевом сайте

1 сентября 2026: все девять целей, размеченных на главной, срабатывают на
`https://www.primeswim.ru/`, счётчик отдаёт 111745879, параметров ни у одной
нет. Проверка сделана с перехватом `reachGoal` — в Метрику ничего лишнего
не отправлялось.

## Полный список целей типа «Целевое событие» (ex JS-событие)

Тип цели в интерфейсе: **JavaScript-событие**, идентификатор — точно как
в столбце «Идентификатор», без пробелов и заглавных.

| Идентификатор | Название для отчёта | Когда срабатывает |
|---|---|---|
| `cta_booking` | Клик «Записаться» | любая кнопка записи: шапка, первый экран, мобильная панель, мобильное меню, карточки тарифов |
| `cta_schedule` | Клик «Расписание» | кнопка расписания на первом экране и ссылка «Всё расписание» в блоке расписания и цены |
| `start_form` | Начал заполнять форму | первое касание любого поля формы, **один раз** на экземпляр формы |
| `lead_delivered` | Заявка доставлена | функция ответила, что заявка ушла в MAX, и назвала её номер |
| `click_telegram_booking` | Написать в Telegram (запись) | личный чат администратора — сюда пишут заявку |
| `click_max_booking` | Написать в MAX (запись) | личный чат в MAX |
| `click_telegram_channel` | Открыл Telegram-канал | публичный канал школы, его читают |
| `click_max_channel` | Открыл канал в MAX | публичный бизнес-канал |
| `click_vk` | Открыл ВКонтакте | публичная страница |
| `click_phone` | Клик по телефону | любая ссылка `tel:` на сайте |
| `click_route` | Построить маршрут | ссылка на карточку в Яндекс Картах |
| `view_contacts` | Дошёл до контактов | блок контактов появился на экране, один раз за загрузку страницы |

## Цели типа «Посещение страниц»

Эти события кодом не отправляются намеренно: у страниц свои адреса, и
считать их ещё и событием значило бы считать одно и то же дважды.

| Название | Условие |
|---|---|
| Просмотр расписания | url: содержит `/raspisanie/` |
| Просмотр стоимости | url: содержит `/price/` |
| Просмотр страницы тренера | url: содержит `/trener/` |
| Просмотр страницы бассейна | url: содержит `/bassein/` |

## Чего в кабинете заводить НЕЛЬЗЯ

**Цель «заявка» по нажатию кнопки или по показу экрана «спасибо».** Заявкой
считается только то, что подтвердил приёмник. Ровно из-за подмены намерения
результатом прежние `lead_submitted` и `open_telegram_draft` завышали число
лидов и занижали CPL — повторять это новой целью незачем.

**Передачу содержимого полей формы.** Имя, телефон, возраст и комментарий —
персональные данные, а согласие посетитель давал на аналитику посещений.

## Что смотреть, когда данные накопятся

Главный вопрос первых недель — где теряются люди:

```
визит → cta_booking → start_form → lead_delivered
```

Теперь эта воронка видна целиком: последний шаг подтверждает сервер, а не
предположение. Разрыв между `start_form` и `lead_delivered` — это либо
сложная форма, либо отказы приёмника; второе видно в логах функции.

Полезно рядом: `start_form` против `view_contacts` — доходят ли до формы
вообще, и `click_telegram_booking` / `click_max_booking` против
`lead_delivered` — не проще ли родителю писать напрямую, минуя форму.

## Персональные данные

Ни одна цель не передаёт параметров: ни имени, ни телефона, ни возраста
ребёнка, ни комментария. Это проверено — в вызове `trackGoal` параметров
нет ни в одном месте кода. Не добавляйте их через интерфейс Метрики и не
включайте передачу содержимого полей форм: это персональные данные, а
согласие посетитель давал на аналитику посещений.

UTM-метки, `yclid` и адрес посадочной страницы Метрика собирает сама —
их сохранять можно и нужно, они не про конкретного человека.
